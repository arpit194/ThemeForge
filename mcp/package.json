{
  "name": "@arpit194/themeforge-mcp",
  "version": "0.1.0",
  "description": "MCP server for ThemeForge — gives Claude knowledge of your token catalog",
  "type": "module",
  "bin": {
    "themeforge-mcp": "./dist/mcp/src/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "release:patch": "npm version patch --tag-version-prefix=mcp-v && git push && git push --tags",
    "release:minor": "npm version minor --tag-version-prefix=mcp-v && git push && git push --tags",
    "release:major": "npm version major --tag-version-prefix=mcp-v && git push && git push --tags"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.28.0"
  },
  "peerDependencies": {
    "themeforge": ">=0.1.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
